import 'package:flutter/material.dart';

class SittingTab extends StatelessWidget {
  static const String routeName='set';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orangeAccent,
    );
  }
}
